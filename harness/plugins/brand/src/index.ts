/**
 * PureGamma browser-brand plugin, host half.
 *
 * The package contributes presentation only; Loader still needs a host-side
 * module while the browser implementation is exported from `./client`.
 */
export function apply(): void {}
