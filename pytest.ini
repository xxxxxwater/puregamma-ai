[pytest]
testpaths = tests
pythonpath = .
addopts = -q --import-mode=importlib
markers =
    contract: expected product contract for a business module that is not implemented yet
    security: security regression tests
